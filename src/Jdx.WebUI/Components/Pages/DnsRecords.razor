@page "/dns"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to new DNS Records settings page
        Navigation.NavigateTo("/settings/dns/records");
    }
}
